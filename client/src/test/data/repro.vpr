// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

domain Matrix {
  function select(m: Matrix, i: Int, j: Int) : Int
  function size(m:Matrix) : Int
}
method rotate(m:Matrix) {

  var x: Int := 0

  while(x < 6)
    invariant true
  {
    var y: Int := 0
    while(y < size(m)) {
      var tmp : Int := 4
      
    }   
  }
  }